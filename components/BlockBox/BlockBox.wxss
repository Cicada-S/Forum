/* components/BlockBox/BlockBox.wxss */
.blockBox {
  width: 100%;
  min-height: 80rpx;
  border-radius: 16rpx;
  overflow: hidden;
  margin-bottom: 20rpx;
  background-color: var(--Color-White);
  box-shadow: var(--Box-Shadow);
}
